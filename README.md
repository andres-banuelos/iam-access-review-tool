# iam-access-review-tool
Automated IAM access review for Azure Entra ID via Microsoft Graph API — flags overprivileged accounts, orphaned users, stale permissions, and excessive service principal permissions. Outputs professional HTML/Markdown audit reports.
